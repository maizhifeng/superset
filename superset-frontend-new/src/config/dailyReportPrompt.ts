/** 日报模板 — AI 根据查询到的数据生成 Markdown 报告 */

const DAILY_REPORT_PROMPT = `## 任务：生成昨日数据日报（对比前日）

你接收到以下 4 组数据，数据列包括 \`report_date_calc\`（日期维度），每个项目在表中有多条记录，分别对应昨日和前日。请基于这些数据生成报告。

### 数据说明

各数据表中的列名与含义：
| 数据列 | 含义 | 模板中对应 |
|--------|------|-----------|
| \`papp_name\` | 项目名称 | 项目 |
| \`cch_name\` | 渠道名称 | 渠道 |
| \`channel_name\` | 媒体名称 | 媒体名称 |
| \`report_date_calc\` | 日期（毫秒时间戳） | 日期 |
| \`SUM(ad_real_cost)\` | 广告消耗金额 | 消耗 |
| \`SUM(n_unum)\` | 新增用户数 | 新增用户 |
| \`cpa\` | 获客成本 | CPA |
| \`roi_1\` | 首日ROI（百分比） | ROI1 |
| \`ltv_1\` | 首日LTV | LTV1 |

### 格式要求
- 数字按可读性自行组织格式，不要出现"0.01万"这类不直观表示
- SUM(n_unum)（新增用户列）：整数显示
- 改变率 = (昨日值 - 前日值) / 前日值 × 100%，显示为 ±X%
- ROI1 值已经是百分比（如 8.95 = 8.95%），直接显示
- 指标速览和项目概览只分析消耗 > 0 的项目；渠道明细请列出前 5 个渠道（含消耗为 0 的）
- **禁止使用 LaTeX 数学语法**（如 $...$、\frac、\approx、\rightarrow 等），所有公式直接用普通文本和 Unicode 符号表达，例如"变化率 = (昨日值 - 前日值) / 前日值 × 100%"

### 报告模板

---

## 📊 核心指标速览

**报告日期**: 昨日 | **对比**: 前日

| 项目 | 日期 | 消耗 | 新增用户 | CPA | ROI1 | LTV1 | 评估 |
|------|------|------|---------|-----|------|------|------|
| 项目A |  |  |  |  |  |  | 🟢/🟡/🔴 |
| 项目A |  |  |  |  |  |  | 🟢/🟡/🔴 |

**说明**: 同一项目会出现两行（不同日期），据此对比变化。计算变化率 = (昨日值 - 前日值) / 前日值 × 100%。

ROI 评估规则：
- 🟢 优秀 ROI1 ≥ 10%
- 🟡 中等 ROI1 5%~10%
- 🔴 需优化 ROI1 < 5%

## 📋 项目表现分析（Top 5 按消耗）

对每个项目，从"项目+渠道明细（昨日）"数据中按 papp_name 筛选，**列出该项目前 5 个消耗最高的渠道**（消耗为 0 的渠道也列出，显示为 0）。

### 1. [项目名称]
**概览**: 消耗 ¥X（前日 ¥X, ±X%）| 新增 X | ROI1 X% | 评估 🟢

| 渠道 | 日期 | 消耗 | 新增用户 | CPA | ROI1 | 评估 |
|------|------|------|---------|-----|------|------|
| 渠道A | 5/25 | X万 | X | X | X% | 🟢 |
| 渠道B | 5/25 | X万 | X | X | X% | 🟡 |
| 渠道C | 5/25 | 0 | X | - | - | - |
| ... |  |  |  |  |  |  |

**分析**: 简要评价该项目表现及变化

### 2. [项目名称]...

## 📺 媒体表现分析

| 排名 | 媒体名称 | 日期 | 消耗 | 新增用户 | CPA | ROI1 | 质量评级 |
|------|---------|------|------|---------|-----|------|---------|
| 1 |  |  |  |  |  |  | 🟢/🟡/🔴 |

评级规则：
- 🟢 优质 ROI1 ≥ 10%
- 🟡 中等 ROI1 5%~10%
- 🔴 需优化 ROI1 < 5%

## 📈 近7天消耗趋势

列出每日总消耗数据，并判断趋势方向。

## 💡 分析与建议

- **整体表现**: 消耗/新增用户/ROI 总结及变化趋势
- **异常提醒**: 消耗或 ROI 波动超过 ±20% 的项目/媒体
- **优化建议**: 基于数据的优化方向

---

## DRILL_DOWN_SUGGESTIONS

报告生成完毕后，根据数据分析结果生成 3-5 条钻取分析建议。每条包含展示标签和完整分析指令，格式为：展示标签 | 完整分析指令
例如：
- 钻取Top5渠道CPA | 请基于项目+渠道维度的数据，重点分析消耗最高的前5个渠道的CPA和ROI质量，指出哪些渠道获客成本偏高及优化方向
- 分析异常波动项目 | 请基于各维度的数据，分析哪些项目或渠道昨日ROI波动超过±20%，以及可能的原因

---

请开始分析数据并生成日报。`;

export default DAILY_REPORT_PROMPT;
