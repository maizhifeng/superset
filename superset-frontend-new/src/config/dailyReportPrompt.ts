/** 日报模板 — AI 根据查询到的数据生成 Markdown 报告 */

const DAILY_REPORT_PROMPT = `## 任务：生成昨日数据日报（对比前日）

你接收到以下 5 组数据，数据列包括 \`report_date_calc\`（日期维度），每个项目在表中有多条记录，分别对应昨日和前日。请基于这些数据生成报告。

### 数据说明

各数据表中的列名与含义：
| 数据列 | 含义 | 模板中对应 |
|--------|------|-----------|
| \`papp_name\` | 项目名称 | 项目 |
| \`cch_name\` | 渠道名称 | 渠道 |
| \`channel_name\` | 媒体名称 | 媒体名称 |
| \`ptid\` | 团队ID | 团队 |
| \`report_date_calc\` | 日期（毫秒时间戳） | 日期 |
| \`SUM(消耗)\` | 广告消耗金额 | 消耗 |
| \`SUM(新增进入)\` | 新增用户数 | 新增用户 |
| \`cpa\` | 获客成本 | CPA |
| \`roi_1\` | 首日ROI（百分比） | ROI1 |
| \`ltv_1\` | 首日LTV | LTV1 |

### 格式要求
- 数字按可读性自行组织格式，不要出现"0.01万"这类不直观表示
- SUM(新增进入)（新增用户列）：整数显示
- 改变率 = (昨日值 - 前日值) / 前日值 × 100%，显示为 ±X%
- ROI1 值已经是百分比（如 8.95 = 8.95%），直接显示
- 指标速览和项目概览只分析消耗 > 0 的项目；渠道明细请列出前 5 个渠道（含消耗为 0 的）
- **禁止使用 LaTeX 数学语法**（如 $...$、\frac、\approx、\rightarrow 等），所有公式直接用普通文本和 Unicode 符号表达，例如"变化率 = (昨日值 - 前日值) / 前日值 × 100%"

### 报告模板

---

## 📊 核心指标速览

**报告日期**: 昨日 | **对比**: 前日

| 项目 | 日期 | 消耗 | 新增用户 | CPA | ROI1 | LTV1 | 评估 |
|------|------|------|---------|-----|------|------|------|
| 项目A |  |  |  |  |  |  | 🟢/🟡/🔴 |
| 项目A |  |  |  |  |  |  | 🟢/🟡/🔴 |

**说明**: 同一项目会出现两行（不同日期），据此对比变化。计算变化率 = (昨日值 - 前日值) / 前日值 × 100%。

ROI 评估规则：
- 🟢 优秀 ROI1 ≥ 10%
- 🟡 中等 ROI1 5%~10%
- 🔴 需优化 ROI1 < 5%

## 📋 项目表现分析（Top 5 按消耗）

对每个项目，从"项目+渠道明细（昨日）"数据中按 papp_name 筛选，**列出该项目前 5 个消耗最高的渠道**（消耗为 0 的渠道也列出，显示为 0）。

### 1. [项目名称]
**概览**: 消耗 ¥X（前日 ¥X, ±X%）| 新增 X | ROI1 X% | 评估 🟢

| 渠道 | 日期 | 消耗 | 新增用户 | CPA | ROI1 | 评估 |
|------|------|------|---------|-----|------|------|
| 渠道A | 5/25 | X万 | X | X | X% | 🟢 |
| 渠道B | 5/25 | X万 | X | X | X% | 🟡 |
| 渠道C | 5/25 | 0 | X | - | - | - |
| ... |  |  |  |  |  |  |

**分析**: 简要评价该项目表现及变化

### 2. [项目名称]...

## 📺 媒体表现分析

| 排名 | 媒体名称 | 日期 | 消耗 | 新增用户 | CPA | ROI1 | 质量评级 |
|------|---------|------|------|---------|-----|------|---------|
| 1 |  |  |  |  |  |  | 🟢/🟡/🔴 |

评级规则：
- 🟢 优质 ROI1 ≥ 10%
- 🟡 中等 ROI1 5%~10%
- 🔴 需优化 ROI1 < 5%

## 👥 分团队分析

从"4. 分团队分析（昨日）"数据中分析。该表按 \`ptid\` + \`cch_name\` 维度列出所有组合，ptid 列即为团队标识，直接使用该字段原值。**数据日期为昨日，报告输出中需注明日期。**

| 团队（ptid） | 渠道 | 日期 | 消耗 | 新增用户 | CPA | ROI1 | LTV1 | 评估 |
|-------------|------|------|------|---------|-----|------|------|------|
| t1 | 渠道A | 6/3 |  |  |  |  |  | 🟢/🟡/🔴 |
| t1 | 渠道B | 6/3 |  |  |  |  |  | 🟢/🟡/🔴 |

评级规则：
- 🟢 优质 ROI1 ≥ 10%
- 🟡 中等 ROI1 5%~10%
- 🔴 需优化 ROI1 < 5%

说明：直接列出所有团队+渠道组合的昨日数据，无需聚合。**输出分析时必须标明日期或数据范围**（如"昨日（6/3）"或"近7天"），report_date_calc 列即为日期。

## 📈 近7天消耗趋势

列出每日总消耗数据，并判断趋势方向。

## 💡 分析与建议

- **整体表现**: 消耗/新增用户/ROI 总结及变化趋势
- **异常提醒**: 消耗或 ROI 波动超过 ±20% 的项目/媒体/团队
- **优化建议**: 基于数据的优化方向

---

## DRILL_DOWN_SUGGESTIONS

报告生成完毕后，根据数据分析结果生成 3-5 条钻取分析建议。

每条建议必须是多维度交叉分析，不局限于单一维度。每条建议的指令中必须要求查看**分天详细数据**，分析每日变化趋势。例如：
- 项目 × 渠道：查看某项目在各渠道的每日消耗趋势，定位消耗突变的日期和渠道
- 团队 × 媒体：按日对比不同团队在同一媒体上的效率变化
- 渠道 × 团队：按日查看高消耗渠道在各团队间的消耗分配变化
- LTV × 渠道：按日对比不同渠道的 ltv_1~ltv_7 曲线，通过 LTV7/LTV1 增长系数评估用户留存质量

输出格式：每行一条，标签和完整指令之间用 | 分隔（不要出现"展示标签"或"完整分析指令"字样）。例如：
A项目渠道效率分化 | 按日查看 A 项目在各渠道间的消耗和 CPA 趋势，分析各渠道每日波动及优化方向

---

请开始分析数据并生成日报。`;

export default DAILY_REPORT_PROMPT;
